# Copy to .env in 1Panel compose dir. Never commit the real secret.
TOKEN_MONITOR_SECRET=PASTE_YOUR_EXISTING_WORKER_SECRET_HERE
# Optional Discord alerts for Watchtower (shoutrrr URL). Leave empty to disable.
# Example (Discord webhook): discord://webhook_id/webhook_token
WATCHTOWER_NOTIFICATION_URL=
